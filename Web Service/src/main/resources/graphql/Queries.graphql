#All queries
type Queries{
    getAllFootballEvent: [FootballEvent],
    findByIdFootballEvent(id: ID): FootballEvent,

    getAllAppUser: [AppUser],
    findByIdAppUser(id: ID): AppUser
}

schema {
    query: Queries
    mutation: Mutations
}